
    1. ќбслуживающа€ система.
    Ёта группа содержит один выпадающий список:
       1. „исло машин.
		O: произвольное число машин
		O1: одна-единственна€ машина
		O2: 2 машины
		O3: 3 машины
		Om: фиксированное число машин

    2. ’арактеристики работ.
    „етыре выпадающих списка.
       1. ѕрерывани€.
		: прерывани€ запрещены
		pmtn: прерывани€ разрешены
       2. ¬рем€ обслуживани€.
		: произвольные длительности обслуживани€
		p_ij=1: единичные длительности обслуживани€
       3. ћоменты поступлени€.
		: моменты поступлени€ всех работ равны нулю
		r_j: заданы произвольные моменты поступлени€ работ
       4. ќтношен€ предшествовани€.
		: не заданы отношени€ предшествовани€
		prec: произвольный ацикличный ориентированный граф
		tree: дерево
		in-tree: вход€щее дерево
		out-tree: выход€щее дерево
		chains: набор цепей

    3. ÷елева€ функци€.
    ќдин выпадающий список (с таким же названием)
		C_max: общий момент завершени€ обслуживани€ C_max = max{C_j}
		L_max: максимальное запаздывание L_max = max{L_j}
		sum(C_j): сумма моментов завершени€
		sum(U_j): число запаздывающих работ
		sum(T_j): суммарное запаздывание
		sum(w_j*C_j): взвешенна€ сумма моментов завершени€
		sum(w_j*U_j): взвешенное число запаздывающих работ
		sum(w_j*T_j): взвешенное суммарное запаздывание